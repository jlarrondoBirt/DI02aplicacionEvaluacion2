import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DI02aplicacionEvaluacion1',
  webDir: 'www'
};

export default config;
